dist: trusty

language: cpp

compiler:
  - g++

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
      - qtbase5-dev

script:
  - export CXX=g++-7
  - gcc -v && g++ -v
  - cd ..
  - mkdir build
  - cd build
  - cmake ../gbemu
  - make -j8
  - make test
